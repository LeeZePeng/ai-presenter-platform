# 协作开发约定

## 开发流程

1. 从最新 `main` 创建短生命周期分支：`feature/<topic>`、`fix/<topic>` 或 `chore/<topic>`。
2. 本地复制 `.env.example` 为 `.env`，只填写个人开发凭据；任何密钥、数据库、上传素材和日志都不得提交。
3. 提交前运行 `pnpm run typecheck && pnpm test && pnpm run build`。
4. 推送分支并发起 Pull Request。至少一名非作者完成审查、CI 全绿后才能合并。
5. 禁止直接在生产 Mac 修改已部署源码。生产发布只能从受保护的 `main` 分支发起。

## 提交与审查

- 每个 PR 只解决一个明确问题，并说明用户影响、验证方式、配置或迁移变化、回滚办法。
- 接口、队列状态、部署流程和鉴权变更必须带测试。
- 不在日志、截图、测试夹具或示例配置中出现真实 token、AK、Cookie、代理凭据。
- 涉及收费 GPU、外部模型、域名或数据删除的代码，审查时必须特别标注。

## 分支保护建议

在远程仓库设置中保护 `main`：要求 Pull Request、至少 1 次批准、通过 `quality` CI、合并前分支为最新，并禁止强制推送与删除。

## 生产发布

管理员在 `/admin` 的“部署发布”页面发起发布。服务只会部署配置好的远程和分支，不接受网页传入命令、仓库地址或分支名；有排队或运行中的任务时拒绝部署。详细流程见 [`docs/DEPLOYMENT.md`](docs/DEPLOYMENT.md)。
